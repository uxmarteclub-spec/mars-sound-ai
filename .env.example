# Modelo para copiar: `cp .env.example .env` e editar `.env` (o `.env` real não vai para o git).
#
# Apenas chave ANON (public) no front. Nunca VITE_* com service_role — isso iria para o bundle.
# Operações admin ficam em backend (Edge Function / servidor), não nesta app Vite.

# Supabase (projeto mars-sound-ai). Sem estas variáveis a app usa dados mock.
VITE_SUPABASE_URL=https://ftezgnflagqhokwbkonw.supabase.co
VITE_SUPABASE_ANON_KEY=sua_chave_anon_aqui
